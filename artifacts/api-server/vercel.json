{
  "version": 2,
  "installCommand": "echo 'pre-built bundle — no install needed'",
  "buildCommand": "echo 'pre-built bundle — no build needed'",
  "outputDirectory": ".",
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
