{
  "version": 2,
  "installCommand": "cd /vercel/path0 && pnpm install",
  "buildCommand": "cd /vercel/path0 && pnpm run typecheck:libs && pnpm --filter @workspace/api-server run build",
  "builds": [
    {
      "src": "dist/vercel.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/vercel.js" }
  ]
}
